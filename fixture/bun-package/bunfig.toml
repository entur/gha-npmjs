[install]
# Wait 5 days (432000 seconds) after a version is published before installing it. Blunts
# supply-chain attacks that rely on a compromised version being installed within hours
# of publish.
minimumReleaseAge = 432000
