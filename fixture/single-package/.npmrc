# Wait 5 days after a version is published before installing it. Blunts supply-chain
# attacks that rely on a compromised version being installed within hours of publish.
# Requires npm >= 12, which mise.toml pins.
min-release-age=5
