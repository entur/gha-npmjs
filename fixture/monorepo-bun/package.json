{
  "name": "gha-npmjs-fixture-monorepo-bun",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture bun monorepo using the workspace: protocol. Never published.",
  "license": "EUPL-1.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build"
  }
}
